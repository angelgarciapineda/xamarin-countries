<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Countries.Cross.ViewModels"
             x:Class="Countries.Cross.Views.Page1">
    <ContentPage.BindingContext>
        <local:ItemViewModel></local:ItemViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Picker ItemDisplayBinding="{Binding langfull }" x:Name="LangEntry" Title="Select a lang" ItemsSource="{Binding LangList }" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>